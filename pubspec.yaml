name: firebase_remote_helper
description: A new Flutter plugin project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  firebase_remote_config: ^2.0.10

dev_dependencies:
  test: ^1.21.3
  lints: ^2.0.0